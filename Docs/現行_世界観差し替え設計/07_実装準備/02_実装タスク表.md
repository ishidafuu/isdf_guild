# 実装タスク表

最終更新: 2026-03-10

この文書は、実装作業を着手順に並べたタスク表である。
最初は「通ること」を優先し、最適化や拡張は後段に回す。

## 1. フェーズ 1. 型定義

- `src/domain/types/` を新設する
- 世界観パック型を作る
- 勢力型を作る
- 人物型を作る
- スタッフ型を作る
- 拠点型と設備型を作る
- 依頼型を作る
- 出発前データ型を作る
- 日報型を作る
- スナップショット型を作る
- ギルド主メモ型を作る
- 共通 ID 型と列挙値を切り出す

## 2. フェーズ 2. サンプルデータ

- `src/data/cyberpunk_minimal/` を新設する
- 世界観パック 1 件を作る
- 勢力 3 件を作る
- 冒険者 4 人から 5 人を作る
- スタッフ 0 人版と 1 人版を作る
- 拠点 1 件を作る
- 設備 1 件から 2 件を作る
- 依頼候補 3 件を作る

## 3. フェーズ 3. 最小縦切り

- 依頼候補を読む関数を作る
- 依頼選択を受ける関数を作る
- `dispatch` を組み立てる関数を作る
- `report` を組み立てる関数を作る
- `snapshot` を組み立てる関数を作る
- `guildmaster_note` 候補を組み立てる関数を作る
- 1 本通すサンプル実行ファイルを作る

## 4. フェーズ 4. 状態更新

- 人物状態更新関数を作る
- 勢力関係更新関数を作る
- 拠点状態更新関数を作る
- `report` から `snapshot` を切る処理を作る
- 次回入力へ `snapshot` を反映する処理を作る

## 5. フェーズ 5. 永続化

- 新仕様側ストア一覧を決める
- 型とストア定義を揃える
- 保存順を決める
- 読み込み順を決める
- 最小縦切りを永続化対応する

## 6. フェーズ 6. AI 接続

- AI 入力コンテキスト型を作る
- `report` 出力契約を作る
- `guildmaster_note` 候補出力契約を作る
- `mission` 出力契約を作る
- AI 失敗時のフォールバックを決める

## 7. フェーズ 7. 試運転

- 成功パターンを回す
- 部分成功パターンを回す
- 失敗パターンを回す
- スタッフあり / なしを比較する
- 次回依頼への持ち越しを確認する
- 調整メモを作る

## 8. 実装順のおすすめ

1. `src/domain/types`
2. `src/data/cyberpunk_minimal`
3. `src/core/mission_flow`
4. `src/core/state_update`
5. `src/storage_v2`
6. `src/ai_contracts`
7. `src/examples`

